# PCM 3.1 独立硬件图层弹窗框架

在原厂 UI 之上叠加自绘弹窗(音量 OSD、提示、对话框),**零刷 flash、零残影、与原厂 UI 物理隔离**。

> [English](volume-osd.md) · **简体中文**

![音量 OSD 画在独立硬件图层上](images/06-overlay-volume-osd.jpg)

*台架实拍:我们自绘的音量 OSD 压在原厂 Jukebox 页之上。底下的原厂页面照常工作 —— 进度条在走、时钟在跳、选中按钮仍高亮。零刷写。*

2026-07-20 台架实证通过:全彩、抗锯齿字体、真圆角透明、拧旋钮实时跟手、松手自动收起。

---

## 1. 核心思路

作为**第二个 gf 客户端**,抢一个 layermanager 池外的空闲 Carmine 硬件图层,在上面独立绘制。

各硬件层有**各自独立的 scanout 缓冲**,所以我们写自己的层**永远碰不到原厂 UI(cid 0x1f)的缓冲和锁** —— 老方案"共享 0x1f"导致的争用、残影、动态页闪烁,在物理层面消失。

**关键架构决定:图层只覆盖弹窗矩形,不做整屏透明层。**
整屏 800×480 的"透明"层实测是整屏发黑。改成 surface 只有弹窗大小 + `gf_layer_set_dst_viewport` 定位后,框外硬件根本不合成我们,隔离更彻底。

---

## 2. 必须知道的硬件事实

这些都是踩坑踩出来的,**不看这一节会白干一整天**。

### 2.1 ★★★ gf 层号被驱动反转:`硬件层号 = 7 − gf层号`

`devg-carmine.so` 三处独立做 `neg rN,r1 / add #7,r1`(`carmine_layer_query@0x12cec`、`set_surface@0x12f86`、`set_dest_viewport@0x13124`)。

| gf 层号 | 硬件层 | 能不能用 |
|---|---|---|
| **5** | **L2** | ✅ **用这个** —— 干净的通用 direct-color 层 |
| 6 | L1 | ❌ 视频采集/W 层。驱动通用路径无条件往 `L1EM bits1:0` 写值,而手册 p.412 写明那两位在 L1 上是 **Reserved** → **高 6 位全死,红色永远出不来**,而且垂直方向会截断 |
| 7 | L0 | ❌ 在 layermanager 管辖内,会被反复 disable,表现为周期性闪烁 |

> 曾经花一整晚在"层 6"上排查"为什么只有蓝绿黑"。根因就是这条反转 —— 我们以为在 L6,其实在 L1。

### 2.2 像素格式是 RGBA5551,**不是** ARGB1555

`gf_layer_query` 报的 `0x1710`(ARGB1555)是**谎报**。驱动对 16bpp 一律写 `LnEC=0b10`,手册 p.430 定义为 "Direct color (16 bits/pixel) **RGBA** mode",§7.3.2 给出:

```
bit15..11 = R    bit10..6 = G    bit5..1 = B    bit0 = A
```

且"输出向 MSB 侧移 3 位"(实测 5bit 值 15→120、31→248,完全吻合)。

```c
#define RGBA5551(r,g,b) ((u16)((((r)&0x1f)<<11)|(((g)&0x1f)<<6)|(((b)&0x1f)<<1)|1u))
/* 纯红 0xF801 / 纯绿 0x07C1 / 纯蓝 0x003F / 纯白 0xFFFF / 透明 0x0000 */
```

`ui_core.c` 的 `ui_rgb()` 本来就是这个排布,零改动即可复用。

### 2.3 透明度能力边界

| 能力 | 有没有 | 说明 |
|---|---|---|
| 逐像素**开/关**透明 | ✅ **白捡** | 不调 `set_blending`(默认模式)时,写 `0x0000` 就是真透明,原厂 UI 直接透出来。圆角/镂空/任意形状都成立 |
| 面板**内部**的抗锯齿/渐变 | ✅ **完整 8bit** | 那是在我们自己的面板底色上做的**软件**混合(`ui_core.c` 的覆盖度模型),不受硬件限制。实测文字边缘完美 |
| 8bit **渐变半透**(半透面板/软投影/软圆角) | ✅ **已打通** | 走 `GF_ALPHA_M1_MAP`(mode `0x00080102`)+ 一个 `GF_FORMAT_BYTE` 的独立 alpha 平面 surface。**配方与唯一致命陷阱见 §2.3.1** |

`gf_layer_set_blending` **只接受 9 个 mode**:
`{0, 0x00040102, 0x00040201, 0x00080102, 0x00080201, 0x04000408, 0x04000804, 0x08000408, 0x08000804}`
传 `SRC_PIXEL_ALPHA`(`0x00010102`)会走错误分支返回 9、**根本不发消息**,静默失效。
注意 `0x00080102`(M1_MAP)**在**白名单里 —— 这就是下面那条 alpha 平面路线走得通的原因。

### 2.3.1 ★★★ alpha 平面:配方,以及那个会浪费你一整天的陷阱

> ⛔ **上车之前先读【陷阱 3】。** 申请一块 alpha 平面是**永久性**的,而全车只有四块;
> 在原厂 911 上抢走一块,原厂倒车距离显示这一整个点火周期都会废掉。**上车请用 `panel_alpha = 255`。**

它带来的是**半透明面板 + 面板上不透明的文字 + 抗锯齿软圆角 + 真投影** —— 这些 1 位路径全都表达不了。

```c
/* 1. 建主 surface, 回读它的【像素】pitch */
gf_surface_create_layer(&surf, &layer, 1, 0, W, H, 0x1710, 0, 0);
gf_surface_get_info(surf, &si);
int pitch = si.stride / 2;              /* 像素数, 不是字节数 */

/* 2. alpha 平面 —— 宽度必须取主 surface 的像素 pitch */
gf_surface_create(&asurf, dev, pitch, H + 1, GF_FORMAT_BYTE /* =8 */, 0, 0);
gf_surface_get_info(asurf, &ai);        /* ai.stride == pitch, 每像素 1 字节 */

/* 3. 绑定 */
gf_alpha_t al = {0};
al.mode = 0x00080102;                   /* M1_MAP | BLEND_SRC_M1 | BLEND_DST_1mM1 */
al.map  = asurf;  al.m1 = 255;  al.m2 = 255;
gf_layer_set_blending(layer, &al);
```

**🚨 陷阱一 —— alpha 平面的 byte stride 必须 64 字节对齐。**
RGB surface 的**字节** stride 驱动会替你补齐到 64 的倍数,但 alpha 平面每像素 1 字节,
按 pitch 建宽时它的 stride 就**等于** pitch,没有任何东西替它补齐。pitch 不是 64 的倍数,
alpha 取样就会逐行错位,整个面板出现**斜向条纹**。

用 2×2 因子实验(pitch × 源视口)测的,每格肉眼判读:

| 主 surface | 像素 pitch | alpha stride | 源视口 | 结果 |
|---|---|---|---|---|
| 500×200 | **512**(=8×64) | 512 | 整块 | **干净** |
| 500×200 | **512** | 512 | 子矩形 372×76 | **干净** |
| 520×220 | **544**(=8.5×64) | 544 | 整块 | **条纹** |
| 520×220 | 544 | 372 | 子矩形 | 条纹 |

可见**子矩形源视口没有问题**,元凶只有对齐。选 surface 宽度时让 pitch 落在 64 的倍数上
(512 和 768 都行,520 和 600 不行)。alpha 平面的填充列(`x >= W`)填 0。

**🚨 陷阱二 —— 每次 push 都要重申 blending。**
`gf_layer_set_surfaces` 会把 blending/alpha-map 的绑定冲掉。只在启动时绑一次,
下一帧硬件就拿着失效的 alpha 指针在取样 —— 同样是条纹,但成因不同。`push_layer()` 里顺序必须是:

```
set_surfaces → set_blending → set_src_viewport → set_dst_viewport
  → set_layer_order → enable → update
```

**每个像素的 alpha 由谁决定:** 让**渲染器**逐像素输出(`ui_core.c` 的 `ui_al[]`)。
**不要**试图从颜色反推(比如"这个像素等于面板色,所以是背景")—— 面板一加渐变,
大部分像素颜色就不再等于面板色,整个背景会变成不透明。

**🚨🚨🚨 陷阱 3 —— 申请 alpha 平面是永久的,而且总共只有四块。**

这个陷阱的代价不是一天,是车主的倒车雷达。

`gdcServerCarmine` 从一个四块的池子(LA0..LA3)里发放 alpha 混合平面。原厂开机就已经占着
其中**三块**,只剩一块空的。而且**释放路径根本走不到**:分配器发出去的索引是 `8..11`,
释放分支的守卫却是 `cmp/hi #3` —— 两个值域不相交,所以一块平面一旦发出去,**到断电为止收不回来**。

于是:你的弹窗在 `panel_alpha < 255` 下**第一次绘制**就把最后那块空平面拿走,并且占住整个点火周期。
之后挂 R,原厂倒车距离显示申请不到平面,它画出来的探测区**肾形几何完好、边缘柔和,但填充是纯黑**。
反过来先触发雷达、后弹窗,整个周期都正常 —— 因为原厂先到先得。
**这种"谁先谁得"的顺序依赖就是一次性资源的签名**,以后再碰到类似现象先往这个方向想。

台架实测方法是直接从 `/proc/<pid>/as` 读服务端**自己的**分配账本
(`账本[k] = *(u32*)(0x080dbd04 + disp*0x0e24 + 56*k + 88)`;`k = 0..7` 是硬件层,
`k = 8..11` 就是 LA0..LA3,值 `> 11` 表示空闲):

| | LA0 | LA1 | LA2 | LA3 |
|---|---|---|---|---|
| 开机时 | 层 0 | 层 4 | 层 5 | **空闲** |
| `panel_alpha = 255` 画两次之后 | 层 0 | 层 4 | 层 5 | **空闲** |
| `panel_alpha = 240` 画一次之后 | 层 0 | 层 4 | 层 5 | **层 6 —— 我们的** |

第三行是在弹窗**早已收起、并且已经发过一次全零 `gf_layer_set_blending` 解绑之后**读的。
平面不会回来。(反过来说那次全零调用本身是无害的:`mode = 0` 会让客户端把服务端第一道门要检查的
那个字节清掉,所以**解绑永远不会触发分配**,只有 `M1_MAP` 那几个 mode 才会。)

⇒ **凡是装在车上的,`panel_alpha` 一律用 255。** 代价是失去半透明面板和抗锯齿圆角,
换回来的是倒车雷达。我们的 U 盘打包器会**拒绝**打包 `panel_alpha` 不是 255 的 `ui.def`,
这道守卫是故意喂坏值验证过会拦下来的。

探针是 `code/common/sh4tools/alphatab.c` —— 只读,约 4.8 KB,打印整张表,并附一行
`SANITY=OK/BAD` 用来在基址不对时**直接报错**,而不是安静地返回一堆看着像数据的噪声。

### 2.4 ★★★ 死锁铁律:`gf_layer_update` 前必须走完整重申序列

```
gf_layer_set_surfaces → set_src_viewport → set_dst_viewport
  → gf_display_set_layer_order → gf_layer_enable → gf_layer_update
```

- **裸调 `gf_layer_update`(不重申)会永久 REPLY-block 在 `gdcServerCarmine`(pid 4104)** —— 没有 pending 变更时它在等一个永远不来的 vsync。
- 实现上把上屏收敛成**唯一出口 `push_layer()`**,新代码不许绕过。

> ### ⚠️ 更正(2026-08-02 / 08-04)—— 本文早前的版本这里是错的
>
> 这一节原本写的是"**`gf_layer_disable` + `update` 同样永久死锁,所以收起不能用 disable,
> 要把可见区刷成 `0x0000`**"。**两句都是错的**,而且这个错误建议在真车上造成了实际故障:
>
> - **`disable` + `update` 不死锁。** 死锁只发生在**没有脏位**的时候,而 `disable` 本身必置脏位,
>   所以必被回复。原来那句是从"裸 update 死锁"外推的,从没实测过。
> - **`disable` + `update` 是**唯一**真隐藏。** 采样 gdc 使能位域 `0x080dbd1c` 的 bit `16+硬件层`:
>   `gf_layer_detach` → 位纹丝不动(**它是空操作**,58 字节、零 `jsr`);
>   单独 `disable` → 位不动(只登记未提交);
>   `disable` + `update` → **位清零**。
> - **靠写 `0x0000` 隐藏根本不是隐藏。** 它依赖混合配置保持不变。任何别的 gf 客户端一碰
>   显示级状态(比如 `gf_display_set_layer_order`),那些"透明"像素立刻被按**不透明黑矩形**扫出来
>   —— 真车上看到的就是这个。
> - **进程死了也不会归还层。** `kill -9` 之后使能位永远是 1,弹窗冻结在屏上直到有东西显式清掉。
>   所以安装/更新工具必须假定前任留下了一块开着的层并主动清场(见 §2.6.5)。

排查:`pidin | grep <名>` 看 **Blocked 列** —— `REPLY 4104` = 卡在 gdc;`REPLY 3` = 卡在串口 `devc-sersci`(另一个坑,见 §5)。

### 2.5 其它

- **dst viewport 其实是对称的:宽和高都是 `x2-x1+1`。**
  ⚠️ 早前版本说高是 `y2-y1` 不加 1。2026-08-04 实测更正:抓"弹窗隐藏"和"弹窗显示"两帧真截图做差分,
  变化区外接框是 **372 × 77**,而 `ui.def` 要的是 **372 × 76** —— 宽度分毫不差、高度多一行,
  说明传 `y2 = y1+H` 会渲染 `H+1` 行。应传 `y2 = y1+H-1`。
  (那多出来的一行在真车上就是面板下沿约 1 像素的溢出。)
- **`gf_layer_update` 返回码是硬编码 0**(`8873c: mov #0,r1`),**不能当成功判据**。
- **库完全不校验格式**:`gf_layer_choose_format@0x88844` 是空桩(`mov #0,r0; rts`)。`gf_surface_create_layer` 返回 0 + stride 合理**只证明客户端算术**,不代表硬件接受。传 BGRA8888 照样"成功",然后硬件按自己的模式扫出垃圾。
- **设备上没有 `libgf.so.1`**。`/proc/boot/` 只有 `libgdcApiCarmine.so`(且它**没有 SONAME 项**,动态链接器纯按 NEEDED 文件名找)。必须把 stub 的 soname 设成 `libgdcApiCarmine.so`,否则重启即丢。
- **`BOXX` 无对齐要求**:dst x 路径全程无掩码,任意像素位置都行。
- 💣 **永远不要调 `gf_layer_set_chroma`**:`carmine_layer_program@0x13640` 在 RGBA 分支里把透明色按 **ARGB1555** 打包写进 LnETC,与实际扫出的 RGBA5551 不一致,key 值必然错位。

---

## 2.6 ★★★★★ 层仲裁 —— 真正决定这套东西能不能上车的部分

前面讲的都是**怎么画**。这一节讲**怎么把层保住** —— 这套框架在真车上出过的每一次故障,根源都在这里。

### 2.6.1 没有归属权,层状态是"后写者赢"

`gf_layer_attach(..., GF_LAYER_ATTACH_PASSIVE)` **两个客户端可以同时成功**。硬件真正扫描用的那份
逐层状态 —— 像素格式、pitch、高度、基址、视口、使能位 —— 存在 gdc 的 `/gdc_shm_inform` 里的
**同一份共享影子记录**,谁后写谁赢。没有仲裁,也不报错。

记录地址:`base + 0xe28 + display*0x5a0 + 硬件层号*120`,字段:
`+0` 状态、`+4` 字节/像素、`+8` 忙、`+12` pitch(像素)、`+16` 高度、
`+20`/`+24` 物理地址(低 28 位;真址 = `0xd0000000 | 值`)。
`shm_open("/gdc_shm_inform", O_RDONLY)` 任何进程都能开(服务端建的时候是 `0777`)——
**所以你可以逐帧监视自己那条记录,判断这块层还是不是你的。** 这个只读检查是下面一切的地基。

### 2.6.2 它造成的故障形态

当别的客户端在你的层还开着的时候改写了记录,硬件就会继续扫**你的**内存、却用**他们的** pitch 和格式。
544 像素宽的缓冲按 800 去读,会斜切成一条噪点带;清零的缓冲按不透明去读,就是一个黑矩形。
在车上这表现为"倒车雷达出来以后音量弹窗就坏了",而且很难定位 —— 因为弹窗的**几何位置一直是对的**。

台架上用纯原厂路径复现:起引擎,再起第二个 gf 客户端 attach 任意一层。日志:

```
[watch] 自标定命中 d0 L2  bpp=2 pitch=544 h=220     <- 我们的
[watch] 层被别人改了 code=2 bpp=2 pitch=800 h=480   <- 别人的, 数字和车上一模一样
```

### 2.6.3 ❌ 死路:"挑一块没人用的层"

最直觉的修法是找一块空闲层。**但它不通用,因为层分布随车型而变。**

台架(Panamera 主机)做过 221 秒重度操作的全层普查,结论是硬件 L6 是唯一一块**零变化、零占用**的
RGB 层 —— 完美候选。刷到车上(911)之后,**雷达探测区全黑、车模消失**,因为
**911 上硬件 L6 正是原厂画 PDC 雷达区和车模的那一层**。台架根本没有 ParkAssist,这件事它永远证不了。

更深的坑:损坏并不是"遮挡"造成的,**是我们的 `disable`**。我们的哑火态在每次收起弹窗时调
`gf_layer_disable`;原厂在 PDC 启动时点亮它一次,又被我们关掉 —— 这就是为什么
**连弹窗没显示的时候雷达区也是黑的**。

> **铁律:不是你独占的硬件层,永远不要 disable —— 而你永远不可能独占。**

### 2.6.4 ✅ 正解:让出协议(照搬原厂自己的优先级模型)

别再问"哪块层是空的",改成照原厂自己的做法:**高优先级功能一激活,低优先级自动挂起。**

| 状态 | 行为 |
|---|---|
| 正常 | **每拍**轮询自己那条影子记录 —— 哑火期同样要看 |
| 记录被改 | **立刻停手** |
| 让出态 | 不 `enable`、不 `disable`、不 `update`、不 `set_surfaces`、不重申。原厂完全接管 |
| 记录连续 2 秒回到空闲 | 自动恢复(防抖,免得和原厂逐帧互抢) |

"空闲" = 记录的基址回到未使用占位值 `0x00100000`,或者仍然是我们自己的 surface。

**🚨 让出动作必须是"什么都不做"。不要先"干净地隐藏一下"。**
很诱人的写法是先 `go_dark()`(disable + update)再停手。那会把同一个 bug 原样带回来:
如果原厂是"功能启动时点亮一次"的模式,而让出恰好发生在它点亮之后,我们这最后一次 `disable`
就把它关掉了,而我们又承诺再也不碰 —— 于是它永远黑着。
检测到位移时记录已经属于原厂、屏上显示的本来就是**他们的**内容,所以单纯停止提交既充分又安全。

真车结果:PDC 区域和倒车影像的表现和原厂完全一致;雷达占用该层期间弹窗消失,雷达一走它自己回来。
**这同时也让"必须知道某车型的层分布"这个前提彻底消失了。**

### 2.6.5 启动时必须假定前任留了一地鸡毛

因为被杀的进程不会归还层(§2.4),每次启动先清场:遍历候选层,凡是记录里还带着**我们的签名**的
(高度 == `UI_MAXH` 且 字节/像素 == 2 —— 原厂层是 480 或 1088 高,撞不上),attach 上去做一次
`disable` + `update`。

```
[purge] 清掉前任残留: gf1 (硬件L6)  h=220 pitch=512
[purge] 清掉前任残留: gf5 (硬件L2)  h=220 pitch=544
[pick]  选中 gf1 = 硬件 L6
```

同时要捕获 `SIGTERM`/`SIGINT`/`SIGHUP`/`SIGQUIT`/`SIGSEGV`/`SIGBUS`,退出前把层关掉。
`kill -9` 拦不住 —— 那种情况正是"清场"存在的理由。

### 2.6.6 收起:顺序很要命

**先关层,再清缓冲**:

```c
go_dark();                 /* disable + update -> 硬件停止扫描我们 */
clear_rgb(); clear_alpha();/* 此时清什么都不会被扫出来           */
```

反过来会在消失瞬间闪一帧**面板形状的黑框**:你在清 RGB 的时候层还活着,而
**alpha 平面还保持着面板那圈不透明值** —— 硬件就照"面板形状 + 全黑颜色"渲染了出来。
清像素这件事本身是 1 位时代的遗留(那时清像素**就是**隐藏手段);现在隐藏靠 `disable`,
清像素只是纵深防御,而且必须发生在层关掉之后。alpha 平面同样要清。

---

## 3. 代码结构

```
code/volume-osd/
  coexist_pop.c    弹窗引擎(本框架的主程序)
  ui_core.c        共享渲染核心 + ui.def 解析器 —— 与 Mac 预览器同一份
  ui_font.h        离线烤的字库(数字 + 比例字体, 含汉字)
  ui.def           弹窗描述(几何/配色/动画/绑定) —— 纯文本, 热加载
  gf_defs.h        gf 常量/结构体/原型(从设备真库反汇编重建)
```

**引擎 / 内容分离**:改布局、配色、位置只要推 **348 字节的 `ui.def`**,引擎每秒查一次 FNV 哈希,变了就重解析重画 —— **不用重编、不用重推 66KB 二进制**。迭代速度差一个数量级。

### 写 `ui.def`

`w`/`h` 描述的是**控件框**;投影边距从里面抠,所以可见面板是 `w - 2*shadow` × `h - 2*shadow`,居中。
(早前版本只从左边和下边抠,导致面板偏心、下沿还露出一条浅带 —— 这个不对称已经去掉了。)
所有元素坐标(`icon_*`、`bar_*`、`num_*`)都是相对**面板**而不是控件框,所以调 `shadow` 时它们不用跟着改。

另外 `ui_icon` 是以 `y + 8` 为中心画的,所以 `icon_y` 给的是图标**顶部**而不是中心。

做 Material 风格时新增的键,全部热加载、不用重编:

| 键 | 含义 |
|---|---|
| `panel_alpha` | 面板底的不透明度 0-255。`255` = 完全不透明,不分配 alpha 平面(等同 1 位路径);小于 255 就启用 §2.3.1 的 alpha 平面 —— **那会永久占掉四块 alpha 平面之一,并让原厂倒车距离显示在整个点火周期内失效。上车必须保持 255,见【陷阱 3】。** |
| `panel2` | 竖向渐变的底色(`panel` 是顶色)。设成和 `panel` 同色 = Material 那种纯色平面 |
| `shadow_a` | 投影浓度 0-255 |
| `shadow_dx` / `shadow_dy` | 投影偏移。Material 风格是 `dx=0`、`dy` 几个像素 |
| `num_scale` | 数字缩放百分比。字模是 23×28,渲染器做面积平均缩小,比最近邻干净得多 |

**投影要做两层,不要一层。** 单层投影在浅色底上不管怎么调都读成一圈生硬的深描边。
Material 的做法是叠加 *ambient*(紧、略深、水平不偏)和 *key light*(宽、很淡、明显下偏),
用覆盖度取大合成,正好复现 elevation 的浓度曲线。用真截图采样发现的两个坑:

- 距离要用**圆角矩形**的(`q = max(|p-中心| - (半尺寸 - R), 0); d = |q| - R`),不能用矩形的,
  否则胶囊形面板外面会露出一圈明显的方框。
- 投影要从**平移后的形状**投出、并画在面板**底下**。平移已经算好的"环"会把它的**内**边界也一起外移,
  面板和投影之间会裂开 2-3 像素的干净背景(实测:面板右边到 x=390,x=391~393 是纯背景,x=394 才开始有影)。

### 渲染流程

```
ui_render()  →  ui_popbuf(RGBA5551 颜色)
             +  ui_cov   (8bit 覆盖度 —— 抗锯齿)
             +  ui_al    (8bit 目标不透明度 —— 面板 vs 内容)
             →  blit_layer(): 色|1, alpha = ui_al * ui_cov / 255
                              (panel_alpha == 255 时退回 1 位路径:
                               cv >= 128 ? 色|1 : 0x0000)
             →  push_layer(): 完整重申 + set_blending + update
```

`ui_cov` 和 `ui_al` 故意分开:覆盖度是**形状**(让边缘平滑),不透明度是**意图**
(面板底半透、画在它上面的文字不透)。blit 时相乘,才能得到"半透面板上的抗锯齿软圆角"。

surface 一次建 `UI_MAXW×UI_MAXH`(520×220),之后靠 **src viewport 裁剪 + dst viewport 定位** —— `ui.def` 改几何时**不需要重建 surface**。

---

## 4. 活体音量链(V4)

拧旋钮实时跟手的数据来源。**原样沿用台架已验证的 `coexist_vol.c` v37,一个常量都没改。**

> `coexist_vol.c` 是独立音量 OSD 的**前身**,已由 `coexist_pop.c` 取代(不随本框架一起发布)。
> 这里点名它,只是为了记录这些常量的出处,以及它们没有被重新推导过。

```
扫堆找 u32 == 0x085c76fc
  且 u32@(X+0x160) == X-0x218
  且 u32@(X-0x218) == 0x085c4c5c        → 得 V
P  = u32@(V+0x168)
ok = u32@(P+0xc8) == 2                   (DATA_OK)
音量 = u8@(P+0x7c)                       (0..40)  ★
src = u32@(P+0x74) ∈ {34,35}             → 铃声, 要丢掉
```

读法:`open /proc/<PCM3Root pid>/as` + `lseek` + `read`,**纯只读,不碰任何 IPC/IOC 通道**(那条把真车和台架都挂死过)。堆扫描范围 `0x0866e200 – 0x08a00000`,64KB 一块。

> ⚠️ **这里的 `0x218` 是结构校验偏移,不是音量。**
> 被证否的那个"0x218 = 主音量"是**持久化文件**里的偏移(真相是 SMS 提示音 slot 9)—— 两个命名空间数字撞车,**别"顺手改正"**。

### 取值层的四道防护

| 防护 | 为什么 |
|---|---|
| **去抖**(连续 2 次同值才认) | idle 时缓存会在 19/20 之间自己抖 → 没人碰旋钮弹窗也会自己蹦 |
| **开机播种**(首个有效值只记不画) | 否则开机、改 ui.def 都会弹一次 |
| **覆盖态日志** | `/tmp/uival` 测试残留会**永久锁死活体链**且毫无提示 |
| **掉线重定位**(连续 10s 读不到才重扫) | 阈值要比一次来电长,否则来电就白扫 3.57MB |

> ⚠️ 别照抄 `coexist_vol.c` 的重扫守卫 —— **那段是死代码**(`int vol=-1;` 从未赋值 → `vol<0` 恒真 → 每 50 拍无条件全堆扫)。

---

## 5. 台架开发闭环(不用插 U 盘)

```bash
# 编译(脚本会在检测到 error: 时中止, 不会留旧二进制骗你)
bash code/volume-osd/build.sh code/volume-osd/coexist_pop.c code/volume-osd/coexist_pop

# 推送(~97 秒 / 66KB), 自动比对 cksum
python3 code/common/serial/ser_push.py code/volume-osd/coexist_pop.stripped /tmp/coexist_pop 192

# 起进程 —— 三个重定向一个都不能少(见下)
python3 code/common/serial/ser2.py 'chmod 755 /tmp/coexist_pop; /tmp/coexist_pop </dev/null >/dev/null 2>/dev/null &'

# 拉日志
python3 code/common/serial/ser_pull.py /tmp/pop.txt pop.txt

# 只改布局/配色: 推 348 字节即可, 不用重编重推
python3 code/common/serial/ser_push.py code/volume-osd/ui.def /tmp/ui.def 192

# 手动指定显示值(测渲染), 删掉文件回到活体音量
python3 code/common/serial/ser2.py 'echo 30 > /tmp/uival'
python3 code/common/serial/ser2.py 'rm -f /tmp/uival'
```

### 串口相关的坑

1. **启动必须 `</dev/null >/dev/null 2>/dev/null`。**
   串口插着没人读 → `devc-sersci`(pid 3)缓冲满 → 任何写控制台的进程**永久 REPLY 阻塞**,伪装成"某个库调用挂死"。这条曾害我们误判 `gf_dev_attach`、反汇编整条 MsgSend 链、白绕数小时。

2. **推完必须比对 cksum**(size 相同不代表内容相同)。
   `ser_push.py` 曾有个潜伏 bug:某块的**第一个字节恰好是 `-`**(可打印所以原样透传),shell 去引号后 `print` 把它**当成选项** → `print: -\: unknown option` → 整块静默丢失。已修(每块首字节一律转义)。
   **定位手法**:本地实现 POSIX cksum,枚举"删掉第 N 块"后的值去匹配台架实测 cksum,一次算出是第几块,再单独发那一块看报错 —— 比二分快得多。

3. **台架 grep 不支持 `|` 或运算**(`grep -i "a\|b"` 静默返回空)。要么分开 grep,要么 `grep -E`。曾害我误判"图形栈没起来"。

4. 台架**没有** `dd` / `cp` / `head` / `wc` / `touch`;`slay` 是交互式的(`-f` 也不管用),用 `code/common/serial/ser_kill.py` 自动应答。

---

## 5.1 用真截图核对,别靠盯屏幕

主机能把**合成后**的整帧交给你 —— 原厂 UI **和**你的 overlay 都在里面 ——
所以绝大多数界面问题都能离线回答,不必让人对着屏幕拍照。

```c
gf_surface_create(&snap, dev, 800, 480, di.format, 0, 0);
gf_display_snapshot(disp, 0, 0, 0, 799, 479, snap);   /* x2,y2 含端点 */
```

快照回来是 **BGRA8888**(`fmt 0x1420`,stride 3200),与各层自己用什么格式无关。
以十六进制文本导出(57600 串口传二进制易丢字节),在主机侧转换。

```bash
/tmp/pcmshot /tmp/s.hex 200 190 400 100     # 指定区域, 全分辨率
python3 ser_pull.py /tmp/s.hex /tmp/s_raw.txt
```

**量几何要用两帧差分。** 抓一张"弹窗隐藏"、一张"弹窗显示",变化像素的外接框**就是** overlay 的真实占位:

```bash
python3 shotdiff.py /tmp/bg_raw.txt /tmp/fg_raw.txt 200 190 372 76
#   实际占位: 372 x 76      期望: 372 x 76      宽 OK / 高 OK
```

dst 视口那个差一行的 bug(§2.5)就是这条命令抓出来的 —— 人眼报告是"大概溢出一个像素",
差分报告是精确的多一行。

**快照看不到的东西:** 硬件扫描阶段产生的问题,比如 §2.3.1 的 alpha 平面条纹。
它们存在于显示控制器里、不在任何缓冲区中,所以**这一类** bug 仍然需要拍照 ——
向别人要照片时要明确说清楚为什么这次非拍不可。

---

## 6. 已经排除的路线(别再试)

| 路线 | 判决 |
|---|---|
| `layermanager.cfg` 的 `reserveLayerForCid` | **断头路**。查到的值存进 `rec+0x2c` 后**没有任何代码再读**,刷了 4 次全无效 |
| `layerOrder` | 被 chip 门挡死。只有 `graphicChip==1` 才应用;本机 CARMINE16=8 → 强制 identity |
| `lastAvailableLayer` 改 `0-7` | **会黑屏**。它是**下限**不是上限,方向反的,改了只剩 slot7 可渲染 |
| 共享原厂 surface 0x1f | 动态页残影是结构性死结,已被本框架取代 |
| BGRA8888 / 32bpp | 该层不支持。库不校验所以"成功"是假的 |
| 各种通道序/端序猜测 | 单个 `0xFFFF` 测量即可判死:全 1 像素在任何位排列下都该出饱和白 |
| **靠台架普查挑层** | **断头路**。层分布随车型而变:硬件 L6 在 Panamera 台架上 221 秒重度操作零变化零占用,在 911 上却是 PDC 雷达/车模层。改用让出协议(§2.6.4),别再找"空闲层" |
| 靠把像素清成 `0x0000` 来隐藏 | 只在没人重置混合配置时成立。别的 gf 客户端一碰显示级状态,你那些"透明"像素就变成不透明黑矩形。隐藏要用 `disable` + `update`(§2.4) |

---

## 7. 相关文件

- 芯片手册:`emulator-lab/docs/carmine-hw-manual.pdf`(MB86297A,L6 寄存器 p.430-432)
  ⚠️ **不要**用 `references/datasheets/MB86296S_CORAL-PA_spec.pdf` —— 那是只有 L0-L5 的前代
- 反汇编配方(两个 .so 都无 section header,`objdump -d` 出空):
  ```bash
  docker run --rm -v "$PWD:/work" sh4gdb:latest \
    objdump -D -b binary -m sh4 -EL --start-address=0x... /work/<path>
  ```
