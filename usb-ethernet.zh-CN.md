# PCM 3.1 · USB 网卡 —— 让白名单里没有的 AX88772B 跑起来

把 57600 串口和来回插拔 U 盘换成一根**网线**,用的还是原厂固件根本不认的卡。
**全程不写 flash** —— 打好补丁的驱动放在硬盘上,挂进第二个 `io-net` 实例。

> [English](usb-ethernet.md) · **简体中文**

2026-08-21 台架实测:`en0` 起来了,192.168.0.90,和 Mac 之间链路已协商。

| 部分 | 内容 |
|---|---|
| [第一部分 — 技术解释](#第一部分--技术解释) | §1 台架为什么需要它 · §2 白名单 · §3 两个互不相干的故障叠在一起 |
| [第二部分 — 技术方案](#第二部分--技术方案) | §4 把 DDK 带进去 · §5 芯片表改一个字节 · §6 完整配方 · §7 跑通长什么样 · §8 工具 |
| [第三部分 — 涉及的问题](#第三部分--涉及的问题) | §9 死路 · §10 教训 |

---

## 第一部分 — 技术解释

### 1. 台架为什么需要它

台架唯一的调试通道是 **57600 波特的串口控制台**。它能用 ——
[`code/common/serial/`](code/common/serial/) 整套串口工具链就是踩在它上面的 —— 但慢到推一个
66 KB 的二进制要一分半,而且脆到每一次推送都得校验 cksum,因为一个 chunk 会静默消失
(串口那堆坑收在 [volume-osd.zh-CN.md](volume-osd.zh-CN.md) 里)。除此以外的一切,都意味着
抱着 U 盘在机器和电脑之间来回跑。

固件那边其实早就准备好了另一条路:`/mnt/ifs1/etc/inetd.conf` 里 **ftp 和 telnet 都是
root 起的**,由 `/HBpersistence/DBGModeActive` 这个空标记文件门控,而台架上它本来就在。
镜像里 USB 以太驱动只有一个,`devn-asix.so`(`devn-rtl.so` 是 PCI 的 RTL8139,不是 USB)。
所以整件事收敛成一句话:**让一张 USB 网卡被认出来**。

### 2. 白名单是精确 VID/PID 匹配,没有通配

`/mnt/ifs1/etc/config/umass-enum-{1000,500}mA.cfg` 按**精确 VID/PID** 绑定 USB 网卡,
四条,没有任何模式匹配:

| VID:PID | 卡 |
|---|---|
| `0b95:7720` | AX88772 |
| `0b95:1720` | AX88172 |
| `2001:3c05` | D-Link DUB-E100 |
| `0846:1040` | NetGear |

cfg 自己的注释还写明了什么不行:*RTL8150, Pegasus, MCS7830 not supported*。

台架上这张卡是 **AX88772B**,报 `0b95:772b`。不在名单里。这不是什么冷门情况:现在市面上
标着 "AX88772" 卖的多半就是 772B,而 Apple 的 USB 网卡(`05ac:1402`,里面也是 ASIX)同样
不在名单里。

`io-usb` 对 772B 的枚举完全正常 —— `usb` 命令能看到 `0b95:772b AX88772B`,说明线、hub、
口全好。它只是从来没被绑到任何驱动上。

**2026-07 对这件事的结论是"买一张原生对得上那四个 PID 之一的卡,别啃 772B"。**
这条结论管了六个星期。这份文档就是撤回它。

### 3. ★★★ 两个互不相干的故障叠在一起

七月那次卡住,原因全在这里。手工挂驱动会**同时因为两个毫不相干的原因**失败,而外面那个
把里面那个盖得严严实实。

#### 3.1 对照组就是全部的诀窍

`mount -Tio-net devn-asix.so` 不报错。它把 `io-net` 直接打成 **SIGSEGV**。试了三次,崩了
三次,**每次都是同一个 `ip`(`0x7827391e`)**。

最顺手的那个解释 ——「驱动不认我们这张没在名单上的卡,于是死了」—— 是错的;而顺着它走的
下一步 —— 去改 PID 表 —— 是个陷阱:改完照样以同样的方式崩,于是你会判定
*PID 补丁没用*,把一条本来是对的路给扔了。

真正能把这件事钉死的动作只要一分钟:**拿没改过的驱动挂一次,看着它以同一个 `ip` 崩掉。**
崩溃和 PID 表毫无关系。到这一步,才轮到问「那还有什么是坏的」。

#### 3.2 问题一 —— 驱动的 `DT_NEEDED` 没写它真正需要的东西

`devn-asix.so` 的 `DT_NEEDED` **只有 `libc.so.2`** 一条。但它引用了一整组 USB DDK 符号:

```
udi_attach  udi_detach  udi_io  udi_enumerate  udi_frame  udi_hcd_info
udi_dma_memory_alloc  udi_dma_memory_free  udi_memory_info
udi_abort_pipe  udi_enumeration
```

挂进一个没有加载 `libusbdi` 的 `io-net`,这些符号一个都解析不到。第一次经由其中任何一个
发起调用,就跳到野地址 —— 那三次崩溃里那个恒定的 `ip`,就是它。

与其说这是驱动写坏了,不如说它从来没有被逼着去声明这个依赖:原厂的加载路径会把 DDK 一起
带上,所以这件事一直没被暴露过。换任何别的方式加载它,DDK 就得你自己喂。

#### 3.3 问题二 —— 驱动内部的芯片表没有 772B

DDK 到位之后,驱动能正常初始化,然后在**业务层**拒绝 —— 这和崩溃完全是两回事:

```
unable to init dll devn-asix: No such device
```

这句拒绝来自驱动自己的芯片表 —— 模块内部的**第二份、独立的白名单**,和 §2 那个
`umass-enum` 的 cfg 不是一个东西。它同样是精确 VID/PID,同样没有 `0b95:772b`。

---

## 第二部分 — 技术方案

### 4. 用 `LD_PRELOAD` 把 DDK 带进去,起第二个 `io-net`

```
LD_PRELOAD=/lib/libusbdi.so.2 ...  /proc/boot/io-net -i1 ...
```

`-i1` 起的是**实例 1**(`/dev/io-net1`),原本在跑的东西一概不动;整个实验被关在一个你自己
起的、也能自己杀掉的进程里。

预载了 `libusbdi` 之后,SIGSEGV 消失,驱动走到了它自己的初始化 —— 于是 §3.3 那句拒绝才
浮出来。**这里的"进展"就是错误信息变好看了**;之前是崩溃把它盖住了。

### 5. 芯片表改一个字节

表是 **16 字节一条记录,`[flags][vid][pid][芯片标记]`,全部小端 u32**。在台架这个二进制里
它从文件偏移 `0x10790` 开始;要看的这一段是:

```
0x107a0  001f1d1f  2001 3c05  00088772   D-Link DUB-E100
0x107b0  000000b0  07d1 3c05  00088772
0x107c0  000000b0  0b95 1720  00088172   AX88172
0x107d0  001f1d1f  0b95 7720  00088772   AX88772
0x107e0  000000b0  0b95 772a  0088172a   AX88772A   ← 改这条
0x107f0  000000b0  1737 0039  00088178
0x10800  3c1c3c8c  0846 1040  00088172   NetGear
0x10810  00130103  13b1 0018  00088772
```

**补丁:`@0x107e8`,PID `0x772a` → `0x772b`,一个字节。** 它是把 AX88772**A** 那条记录
改指过去,而不是新加一条:772B 因此沿用了 772A 的 flags(`0x000000b0`)和芯片标记
(`0x0088172a`)—— 表里最近的那个兄弟 —— 也不用去维护什么记录条数或终止项。

还有第二个变体,改的是 AX88772 那条(`@0x107d8`,`0x7720 → 0x772b`,沿用 `0x001f1d1f` /
`0x00088772`)。当初是当备胎做的,一直没用上 —— 变体 A 就通了。

> 别把 `0x107e8` 硬编到你自己的固件上。
> [`code/usb-ethernet/patch_asix_pid.py`](code/usb-ethernet/patch_asix_pid.py) 是按特征
> **搜**这张表的,写之前先校验旧值,并把改动的每一个字节都打出来。它产出的两个变体,与当初
> 在台架上跑的那两个镜像**逐字节一致**。

### 6. 完整配方

零 flash 写入。驱动就是硬盘上一个普通文件。

```sh
# 机器上 —— 打好补丁的驱动,换个新名字,原厂那份一个字节都不动
cp <打好补丁的驱动> /mnt/data/drv/devn-asixa.so

# 第二个 io-net 实例:预载 USB DDK,并把硬盘目录放进库搜索路径
LD_PRELOAD=/lib/libusbdi.so.2 LD_LIBRARY_PATH=/mnt/data/drv \
  /proc/boot/io-net -i1 -dasixa speed=10,duplex=1 -ptcpip-v4 &

ifconfig en0 192.168.0.90 netmask 255.255.255.0 up
```

```sh
# Mac 这边 —— 需要管理员权限
sudo ifconfig <网口> inet 192.168.0.20 netmask 255.255.255.0 up
```

> 🚨 **台架只有一个 USB 口。** 网卡和 U 盘插不到一起,所以驱动必须**先落到 `/mnt/data`
> (硬盘,持久)上,再把 U 盘换成网卡**。这个顺序搞反,你就会手握一个送不进去的补丁驱动。
> §8 里的 `fx` 也正是为此而生 —— 文件一旦在硬盘上,后面再改字节就完全不用 U 盘了。

### 7. 跑通长什么样

```
/dev/io-net1: en0 ip0 ip_en
en0: flags=8a43<UP,BROADCAST,RUNNING,ALLMULTI,SIMPLEX,MULTICAST> mtu 1500
     address: 00:6f:00:01:01:be
     inet 192.168.0.90 netmask 0xffffff00 broadcast 192.168.0.255
```

Mac 那一侧:

```
media: autoselect (10baseT/UTP <full-duplex>)   status: active
```

关键是 `status: active` —— PHY 和对端协商上了,说明这是一条真的链路,不只是一条存在的
接口记录。

### 8. 工具

| | |
|---|---|
| [`code/usb-ethernet/patch_asix_pid.py`](code/usb-ethernet/patch_asix_pid.py) | 从**你自己的** `devn-asix.so` 生成打好补丁的驱动:按特征搜芯片表、旧值对不上就拒绝、打印改动的每个字节以及撤销用的 `fx` 命令 |
| [`code/usb-ethernet/fx.c`](code/usb-ethernet/fx.c) | 机器上的文件字节读/写工具,带"旧值必须对得上"的闸,于是改一个字节的实验不用再跑一趟 U 盘 |
| 驱动二进制 | **不含** —— `devn-asix.so` 是保时捷专有固件。从你自己的 dump 里抠出来,补丁生成器拿它当输入 |

---

## 第三部分 — 涉及的问题

### 9. 死路

| 做法 | 判决 |
|---|---|
| **「买一张原生对得上那四个 PID 的卡」**(2026-07 的结论) | **已撤回。** 想少花力气的话它是句好建议;但"772B 不可能用"这件事不成立。这条路是被一个有两个成因的症状,给判死了六个星期 |
| 什么都不管,先去改 PID 表 | **就是那个陷阱。** 改完照样崩,因为崩溃是 DDK 缺失造成的。于是你会判定 PID 补丁没用,把一个正确的修法扔掉 |
| 换挂载参数 —— `busnum` / `devnum`、`speed` / `duplex`、`verbose` | **全都没用,三次,每次同一个 `ip`。** 参数修不了一个没解析上的符号;崩溃发生在第一次经由它发起调用的时候 |
| 把驱动挂进现有的 `io-net` | 别。用 `-i1` —— 第二个实例能把失败关在你自己起的进程里 |
| 把崩溃读成「驱动拒绝了我们这张卡」 | 看一眼就知道不对:拒绝长成 `unable to init dll devn-asix: No such device`,是一个活着的进程打出来的。SIGSEGV 不是一个策略判断 |

### 10. 教训

- **动手修之前先跑对照组。** 拿*没改过*的驱动挂一次、看着它以同样的方式失败,只要一分钟,
  而这一分钟就是把一个解不开的问题劈成两个简单问题的地方。没有它,一个正确的 PID 补丁看起来
  就是一个失败的 PID 补丁。
- **叠加故障会把里面那个盖得一点不剩。** SIGSEGV 一点也没提示芯片表,芯片表也一点没提示
  链接问题。第一个修法生效的唯一信号,是**错误信息变了** —— 从一次崩溃变成了一句话。
- **`DT_NEEDED` 不是依赖清单。** 它只是有人记得声明的那部分。一个只会被单一宿主加载的模块,
  完全可以什么都不声明;换个方式加载它,它不会抱怨,它会 fault。
- **一个字节,而且是在硬盘上。** 这里没有任何东西碰过 flash,所以判断错了的代价只是杀掉一个
  进程 —— 这也正是"换张卡算了"六个星期之后,它仍然值得一试的原因。

---

## 附录

### 参考

- 这个问题此前的状态:2026-07 那次台架普查,四个 PID 的白名单和"买一张对得上的卡"的判决,
  都出自那里。
- 被它替掉的串口闭环:[`code/common/serial/`](code/common/serial/) 与
  [`code/common/sh4tools/SERIAL_LOOP.md`](code/common/sh4tools/SERIAL_LOOP.md)。
